JSON data drawer using Python pygame
========

## Usage

* Just type `python3 main.py json_path` to see your image
* To save your image as png add -o or --output flag like this: `python3 main.py -o save_path json_path`
